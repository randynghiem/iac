---
- hosts: localhost
  gather_facts: true
  vars:
    resource_group: "poc"
    location: "southeastasia"
  collections:
    - randynghiem.azure_dev
  tasks:
    - import_role:
        name: health_check
  roles:
    - role: azure_core_infra
